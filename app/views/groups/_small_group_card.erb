<div class="small-card">
  <%= link_avatar group, size: 64 %>

  <main>
    <%= link_to group.name, group, class: [:normal, :title] %>
    <%= link_to("Leave Group", user_leave_group_path(current_user, group), class: "red button") if !can_edit_group? %>
  </main>
</div>
