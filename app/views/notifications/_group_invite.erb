<% group = notification.entity %>

<div class="notif group-invite" fid="<%= notification.id %>">
  <%= link_avatar group, size: 90 %>
  <span><%= group.name %></span>

  <%= link_to "", notification_updated_path(notification, "accepted"), method: :post, remote: true, class: "confirm" %>
  <%= link_to "", notification_updated_path(notification, "denied"), method: :post, remote: true, class: "deny" %>
</div>